#ifndef SUPPORT_FUNCS_HPP
#define SUPPORT_FUNCS_HPP

namespace supply
{
// check that string consists of digits
bool isDigitString(const std::string&);

// replace odd elements in string with FILLER
void replaceEven(std::string&);

} // namespace supply

#endif // SUPPORT_FUNCS_HPP
